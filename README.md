# ExamMonitor

A full-stack application for personalized exam timetables.

## Setup

1. Install Node.js dependencies:
   ```bash
   npm install
   ```
2. Configure `.env` file with your database URL and JWT secret.
3. Start the application:
   ```bash
   npm run dev
   ```

## Requirements
See `package.json` for frontend and backend dependencies.
